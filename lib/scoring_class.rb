module Scrabble

  class Scoring

    SCORE_CHART = [{}]

    def self.score(word)
      #grab word from user, into array_of_words
      #split word into characters array
      #make new array of characters
      #convert chars to INT using each_index?
      #sum all numbers from letters in word
      #stores in array of scores
    end

    def score_keeper
      #grab score from scores array
      #create an index of words played


    end


    def self.highest_score_from(array_of_words)
      #.length or max_by to find
      #longest word in array_of_words
      #return longest word

    end
  end
end
